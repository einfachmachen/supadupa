{
  "name": "mybudgettracker-dual",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:prod": "node server.js --prod",
    "start:old": "node server.js --only=old",
    "start:new": "node server.js --only=new",
    "build:new": "cd new && npm install && npm run build",
    "install:new": "cd new && npm install"
  },
  "dependencies": {
    "express": "^4.21.2",
    "vite": "^5.4.10"
  }
}
